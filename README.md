### To turn on the front end client side server you need to use command:

npm start or serve -s build

Recommend to use serve -s build

### Important notice

If you happen to get error about not having serve, then use this command:

npm install -g serve